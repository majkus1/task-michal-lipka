# Statscore recruitment task sandbox

Use this repository to complete 3 designed tasks - you'll find them in `apps/web` project.

To run the web app navigate to `apps/web` and type `npm run dev` - ofc you have to `npm i` in the root directory. To start the backend, do the same - for `backend` directory ofc.

From now on, you are the owner of this repo - you can do whathever you want - including installing new packages, editing packages config `scripts` section etc.

Good luck!

## Task 1 TDD commit history

One-line history from branch `task1-tdd`:

```text
e6f4f6f task1(tdd): add failing test for table headers
79f57c2 task1(tdd): render table headers
26f1a4a task1(tdd): add failing test for people rows
68dcd10 task1(tdd): render people rows
1bb04af task1(tdd): add failing test for empty state
3e698ba task1(tdd): render empty state
17b5ff1 task1(tdd): add failing test for task one view data flow
baadc69 task1(tdd): connect task one view with api
285a3bf task1(refactor): clean up task one implementation
```